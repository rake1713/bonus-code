#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;

    int i = 2;
    while (i <= N) {
        if (N % i == 0) {
            cout << i << endl;
            break;
        }
        i++;
    }

    return 0;
}
