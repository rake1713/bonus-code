#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int x, y, z, c;
    cin >> x >> y;
    cin >> z >> c;
    if (x == z || y == c || 
        abs(x - z) == abs(y - c)) {
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;
    }

    return 0;
}
