#include <iostream>
using namespace std;

int main() {
    int k, n;
    cin >> k >> n;

    int x = (n - 1) / k + 1;
    int y = (n - 1) % k + 1;

    cout << x << " " << y << endl;

    return 0;
}
