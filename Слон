#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int x, y, z, a;
    cin >> x >> y;
    cin >> z >> a;
    if (abs(x - z) == abs(y - a)) {
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;
    }

    return 0;
}
