#include <iostream>
using namespace std;

int main() {
    long long n, m;
    cin >> n >> m;

    long long x = m / n;
    if (m % n != 0) {
        x++;
    }

    cout << x << endl;
    return 0;
}
