#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    int start = ceil(sqrt(a)); 
    int end = floor(sqrt(b));   
    for (int i = start; i <= end; i++) {
        cout << i * i << " ";
    }
    cout << endl;
    return 0;
}
