#include <iostream>
using namespace std;

int main() {
    long long N;
    cin >> N;

    long long sum;
    if (N > 0) {
        sum = (N * (N + 1)) / 2;
    } else if (N < 0) {
        sum = (N * (N - 1)) / 2;
    } else {
        sum = 0;
    }

    cout << sum << endl;
    return 0;
}
