#include <iostream>
using namespace std;

int main() {
    int x;
    cin >> x;
    bool isLeap = (x % 4 == 0 && x % 100 != 0) || (x % 400 == 0);

    if (isLeap) {
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;
    }

    return 0;
}
